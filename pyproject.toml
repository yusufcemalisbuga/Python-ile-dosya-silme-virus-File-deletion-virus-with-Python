[project]
name = "file-cleaner"
version = "0.1.0"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--strict-markers -v --tb=short --cov=file_cleaner --cov-report=term-missing"
